# E2E-план для системы регистрации через Gherkin и behave

Документ фиксирует, как должна тестироваться будущая система регистрации до и после реализации, и закрепляет, что сценарии описываются в Gherkin и исполняются через `behave`.

Связанный канонический документ:

- [docs/README.md](/workspaces/kdg80/docs/README.md)
- [docs/registration-system-requirements.md](/workspaces/kdg80/docs/registration-system-requirements.md)
- [docs/behave/registration-system.feature](/workspaces/kdg80/docs/behave/registration-system.feature)

## 1. Цели тестового контура

- Проверить пользовательский путь регистрации через браузер на каждом событии. — Статус: `Зафиксировано`
- Проверить sharable event-links `/sobytiya/<slug>/` и registration-intent URLs `/sobytiya/<slug>/?register=1`. — Статус: `Зафиксировано`
- Проверить все обязательные валидации: корректные, некорректные и мусорные данные. — Статус: `Зафиксировано`
- Проверить дефолтный путь `полное ФИО` и отдельную бережную к конверсии ветку `У меня нет отчества`, чтобы двухсловное имя не проходило молча, но и не резало конверсию для людей без отчества. — Статус: `Зафиксировано`
- Проверить конкурентную запись на последние места. — Статус: `Зафиксировано`
- Проверить генерацию билета, PDF и Telegram-уведомления суперадмину. — Статус: `Зафиксировано`
- Проверить поведение системы при временной недоступности Telegram. — Статус: `Зафиксировано`

## 2. Принципиальная среда для E2E

- Полный E2E для Telegram webhook должен гоняться не только локально, а на публичном Fly staging URL с HTTPS. — Статус: `Зафиксировано`
- Локально допускаются только частичные тесты формы и API с моками Telegram. — Статус: `Зафиксировано`
- Для E2E желательно отдельное staging-приложение и отдельный test bot, чтобы не засорять production-чат. — Статус: `Зафиксировано`
- Публичная форма в E2E должна отправлять запрос напрямую на Fly staging API, а не в обход через локальные моки формы. — Статус: `Зафиксировано`
- Для day-1 MVP production-like E2E может использовать штатный `.fly.dev` HTTPS URL приложения, если custom domain на Fly ещё не подключён. — Статус: `Зафиксировано`
- Для статического сайта на Yandex Object Storage production-like browser E2E должен иметь same-origin preview-path на основном домене `kgd80.ru`, а не только `localhost`, иначе проверки `states.json`, ticket URLs и CORS-поведения теряют смысл. — Статус: `Зафиксировано`
- Hidden preview для E2E должен публиковаться из собранного `site/dist`, а не из legacy placeholder-файлов в корне репозитория. — Статус: `Зафиксировано`
- Preview-публикация должна сохранять реальные nested routes сайта, включая `/programma/` и тематические страницы, чтобы Playwright работал по тем же URL, что и пользовательский сценарий. — Статус: `Зафиксировано`
- При деплое preview в секретную подпапку root-absolute site URLs должны переписываться под preview-prefix, но same-origin пути `/tickets/registration/states.json` и публичные `/tickets/<public_hash>/` должны оставаться в корне домена. — Статус: `Зафиксировано`
- Публикация в новый secret preview-prefix не должна требовать bucket-wide `ListObjects/Delete`; для одноразовых preview допустим put-only upload без предварительного listing bucket root. — Статус: `Зафиксировано`
- Скрипты preview/E2E должны уметь читать локальные секреты как из корневого `.env`, так и из `docs/.env`, потому что в текущем workspace тестовые и YC-ключи хранятся именно там. — Статус: `Зафиксировано`

### Production-first hygiene

- Для day-1 допустим production-first smoke/E2E, но только по выделенным test events, не участвующим в реальной продаже мест. — Статус: `Зафиксировано`
- Полный destructive и конкурентный suite не должен бить по реальным production events. — Статус: `Зафиксировано`
- Каждый автоматический прогон должен иметь уникальный `test_run_id`. — Статус: `Зафиксировано`
- После завершения production-like прогона тестовые данные должны очищаться отдельной superadmin-only командой `/cleanup_test_run <run_id>` или эквивалентным maintenance-сценарием. — Статус: `Зафиксировано`

## 3. Инструменты

- BDD runner: `behave`. — Статус: `Зафиксировано`
- Browser E2E: Playwright. — Статус: `Зафиксировано`
- Telegram assertions: Telethon с сессией из `TELEGRAM_SESSION_S22`. — Статус: `Зафиксировано`
- Дополнительные сервисы очередей для тестов не нужны. — Статус: `Зафиксировано`

### Формат сценариев

- Сценарии являются source of truth в Gherkin-файлах для `behave`. — Статус: `Зафиксировано`
- Playwright и Telethon вызываются из step definitions, а не через отдельный неформализованный набор скриптов. — Статус: `Зафиксировано`
- Каждый Gherkin-сценарий должен читаться как воспроизводимый тестовый скрипт: кто именно что делает на сайте, в Telegram или в тестовом окружении. — Статус: `Зафиксировано`
- Основные акторы Gherkin-сценариев: `посетитель сайта`, `суперадмин в Telegram`, `оператор в Telegram`, `тестовый scheduler`, `клиент аварийного export endpoint`. — Статус: `Зафиксировано`
- Архитектурные и внутренние утверждения вида `backend generated ...`, `database stores ...`, `outbox contains ...` не должны быть основным текстом пользовательского Gherkin-сценария. — Статус: `Зафиксировано`
- Такие внутренние проверки допустимы как скрытые assertions внутри step definitions или как отдельные integration checks, привязанные к пользовательскому сценарию. — Статус: `Зафиксировано`

### Замечание по секретам

- `TELEGRAM_SESSION_S22` должен храниться только локально в `.env`-файле, не попадающем в git. — Статус: `Зафиксировано`
- В текущем состоянии репозитория `docs/.env` не отслеживается git и покрывается правилом `.env` из корневого `.gitignore`. — Статус: `Зафиксировано`

## 4. Минимальный тестовый датасет

Нужны как минимум такие события:

- `Тест Научная библиотека — валидация формы` — открытое тестовое событие для негативных проверок формы. — Статус: `Зафиксировано`
- `Тест Научная библиотека — дефолтный овербукинг` — событие с физической вместимостью и стандартной квотой `+63%`, чтобы проверить, что sold-out не наступает ровно на вместимости зала. — Статус: `Зафиксировано`
- `Тест Третьяковка — кастомный овербукинг` — событие с точечным override overbooking-процента `+20%`, чтобы проверить исключение относительно дефолтных `+63%`. — Статус: `Зафиксировано`
- `Тест Последнее место` — событие, где остался `1` билет. — Статус: `Зафиксировано`
- `Тест Фридландские ворота — мест нет` — событие без мест. — Статус: `Зафиксировано`
- `Тест Архив — прошедшее событие` — событие в прошлом. — Статус: `Зафиксировано`
- Не меньше одного открытого события на каждую площадку из матрицы залов. — Статус: `Зафиксировано`

## 5. Базовые E2E-сценарии

### A. Bootstrap Telegram-бота

1. Первый тестовый пользователь отправляет `/start`.
2. Проверяем, что он становится суперадмином.
3. Проверяем, что после `/start` бот показывает кнопочную навигацию.
4. Проверяем, что команда `/help` выводит список доступных команд.
5. Второй тестовый пользователь отправляет `/start`.
6. Проверяем, что второй пользователь не становится админом и получает отказ/нейтральный ответ.

Ожидаемый результат:

- в таблице `telegram_admin` только одна запись;
- суперправа закреплены за первым пользователем.

### B. Happy path регистрации на каждом открытом событии

Для каждого открытого события в текущем каталоге:

1. Playwright открывает `/programma/`.
2. Прокручивает к нужному месяцу программы.
3. Находит конкретную карточку события по названию и площадке.
4. Нажимает CTA `Регистрация`.
5. Проверяет, что открылась форма регистрации с summary выбранного события.
6. Заполняет валидные `ФИО`, `email`, `телефон`.
7. Отправляет форму и убеждается, что submit уходит на Fly registration API по HTTPS.
8. Проверяет success-state и открытие `/ticket/<public_hash>` на основном домене сайта.
9. Проверяет наличие короткого `ticket ID`.
10. Проверяет кнопку `Скачать PDF`.
11. Проверяет блок `Добавить в календарь`.
12. Проверяет доступность `Android (ICS)`, `Календарь Apple`, `Google`.
13. Telethon ждёт уведомление в чате суперадмина.

Ожидаемый результат:

- регистрация создана;
- билет создан;
- публичный билет доступен по длинному `public_hash`;
- пользователю показан `short_ticket_id`;
- на билете есть полный адрес площадки;
- на билете есть блок `Добавить в календарь`;
- на билете доступны platform-friendly варианты календаря, а не одна универсальная непонятная ссылка;
- в сообщении бота есть ФИО, название события, дата/время и остаток мест.

Для читаемости behave-сценариев и Playwright-step definitions используются конкретные публичные события из мастер-файла, например:

- `Виштынецкая возвышенность: освоение с 1945 года, современность и перспективы` — `/programma/` -> `Апрель 2026` -> Научная библиотека
- `Советское монументальное искусство на территории Калининградской области` — `/programma/` -> `Апрель 2026` -> Музей «Фридландские ворота»
- `Разговоры о море` — `/programma/` -> `Апрель 2026` -> Музей Мирового океана
- `Памятники искусства и истории в ландшафте Калининградского зоопарка` — `/programma/` -> `Июнь 2026` -> Филиал Государственной Третьяковской галереи в Калининграде

События ИЦАЭ на старте production в happy-path список не входят: они покрываются отдельным holdback-сценарием, где публично должны показываться только дата, нейтральная служебная строка и CTA `Регистрация скоро откроется`. — Статус: `Зафиксировано`

### C. Валидация корректных и некорректных данных

Отдельные Playwright-сценарии:

1. Пустое ФИО.
2. ФИО из пробелов.
3. Фамилия и имя без отчества при неотмеченной галочке `У меня нет отчества`.
4. Email без `@`.
5. Email с опечаткой в домене.
6. Email из disposable-domain списка.
7. Телефон не на `+7`.
8. Телефон короче и длиннее допустимого.
9. Поля с мусором: HTML, JS, SQL-like строки, emoji, очень длинные строки.
10. Дубль email.
11. Дубль телефона.
12. Submit без галочки согласия на обработку ПДн.

Ожидаемый результат:

- пользователь видит понятную inline-ошибку;
- сервер не создаёт регистрацию;
- в логах и UI нет XSS/unsafe rendering.

### C1. Полное ФИО по умолчанию и разрешённый сценарий без отчества

1. Посетитель открывает форму регистрации на тестовом событии.
2. Проверяет, что поле имени остаётся одним основным input, а сразу под ним находится checkbox `У меня нет отчества`.
3. Вводит фамилию и имя без отчества.
4. Не отмечая checkbox, отправляет форму.
5. Проверяет, что регистрация не создаётся, а inline-ошибка прямо предлагает отметить `У меня нет отчества` или ввести полное ФИО.
6. Отмечает checkbox `У меня нет отчества`.
7. Проверяет, что уже введённые фамилия и имя не теряются, а helper text или placeholder переключается на пример без отчества.
8. Повторно отправляет форму с тем же двухсловным именем.
9. Проверяет, что форма больше не блокируется на этом поле и запрос уходит в registration backend.
10. Отдельно проверяет, что полное ФИО из трёх смысловых блоков проходит и без checkbox.

Ожидаемый результат:

- двухсловное имя не проходит как молчаливый дефолтный путь;
- для пользователя без отчества есть явный и быстрый путь без разбиения формы на три отдельных инпута;
- переключение checkbox не сбрасывает уже введённое имя;
- тексты и фокус-менеджмент помогают закончить регистрацию, а не заводят пользователя в тупик.

### C2. Submit feedback и защита от “нажал и ничего не происходит”

1. Посетитель открывает форму регистрации на конкретном событии.
2. Заполняет валидные данные.
3. Нажимает submit.
4. Проверяется, что кнопка сразу меняет состояние на `Регистрируем…` и становится disabled.
5. При искусственно замедленном ответе сервера проверяется промежуточный текст о резервировании места.
6. Проверяется, что повторный клик не создаёт второй submit.

Ожидаемый результат:

- пользователь видит явный loading-state;
- форма не зависает молча;
- повторная отправка из-за нетерпеливого double-click не создаёт дубль.

### C3. Direct event-link и auto-open registration intent

1. Playwright открывает `/sobytiya/<slug>/` для открытого события.
2. Проверяет, что страница рендерит self-contained информацию о событии и активную CTA `Регистрация`.
3. Затем Playwright открывает `/sobytiya/<slug>/?register=1`.
4. Проверяет, что та же leaf-страница события открывается с автоматически поднятой формой регистрации.

Ожидаемый результат:

- sharable event-link работает без необходимости искать событие в ленте;
- пользователь всегда сначала видит информацию о событии и может решить, регистрироваться или нет;
- `?register=1` не создаёт отдельную каноническую страницу, а только ускоряет intent-flow.

### C4. Share / copy direct registration link from the form

1. Playwright открывает `/sobytiya/<slug>/?register=1` для открытого события.
2. На mobile проверяет наличие secondary-action `Поделиться` в header формы и вызов системного share API с прямым registration-intent URL.
3. На desktop проверяет наличие secondary-action `Скопировать ссылку` и копирование того же URL в clipboard.
4. После desktop-copy проверяет краткое подтверждение `Ссылка скопирована`.
5. При принудительном отказе clipboard API проверяет появление fallback-блока с readonly URL для ручного копирования.
6. После нажатия `Закрыть` проверяет, что `?register=1` исчезает из адресной строки, а пользователь остаётся на leaf-странице события.

Ожидаемый результат:

- на mobile пользователь получает привычный системный share dialog;
- на desktop пользователь сразу получает готовый URL в буфере обмена без лишнего popover;
- при отказе clipboard permissions форма не заводит пользователя в тупик и предлагает ручное копирование;
- `Закрыть` работает как возврат к описанию события, а не как неожиданный переход на другую страницу.

### D. Запрет регистрации на прошлое событие

1. Playwright открывает `Тест Архив — прошедшее событие`.
2. Проверяет отсутствие активной регистрации или серверный отказ при прямом POST.

Ожидаемый результат:

- UI показывает, что событие прошло;
- прямой POST тоже отклоняется на сервере.

### D2. Дефолтный overbooking не закрывает регистрацию ровно на вместимости зала

1. `Тест Научная библиотека — дефолтный овербукинг` имеет физическую вместимость `X` и регистрационную квоту `X + 63%`.
2. Внутренний счётчик доведён до `seats_taken = X`.
3. Playwright открывает карточку события и leaf-страницу.
4. Проверяется, что CTA всё ещё позволяет регистрацию.
5. Затем создаётся ещё одна валидная регистрация.

Ожидаемый результат:

- событие не переходит в `sold_out` в момент достижения физической вместимости зала;
- sold-out наступает только по достижении `registration_limit`;
- public state и Telegram показывают остаток именно регистрационной квоты.

### D3. Точечный override overbooking-процента меняет registration_limit

1. `Тест Третьяковка — кастомный овербукинг` имеет физическую вместимость `X` и custom registration_limit, рассчитанный не по дефолтным `63%`, а по исключению Третьяковки `20%`.
2. Playwright и внутренние проверки читают public state / event-card.
3. Проверяется, что событие остаётся открытым до исчерпания custom registration_limit.
4. После достижения custom registration_limit прямой POST отклоняется.

Ожидаемый результат:

- per-event override реально меняет квоту регистрации;
- UI, API, manifest и операторские отчёты используют один и тот же registration_limit;
- изменение квоты не требует переписывать физическую вместимость зала.

### E. Продажа последнего места регистрационной квоты

1. `Тест Последнее место` имеет ровно `1` свободное место.
2. Два или более параллельных клиента одновременно отправляют форму.

Ожидаемый результат:

- ровно одна регистрация успешна;
- остальные получают `мест нет` или мягкий retry-ответ, если конфликт был до финальной проверки;
- отрицательных остатков нет.

### F. Массовая конкурентная попытка

1. Для события с лимитом запускается пачка параллельных POST-запросов.
2. Количество попыток превышает число свободных мест.

Ожидаемый результат:

- итоговое число регистраций строго равно доступной квоте;
- `seats_taken` не превышает `registration_limit`;
- база не уходит в долгий lock.

### G. Manifest-first доставка состояний регистрации

1. На same-origin домене сайта опубликован актуальный `/tickets/registration/states.json`.
2. Playwright открывает страницу `/programma/` с несколькими карточками событий.
3. Проверяется, что CTA-состояния берутся из state-file.
4. Проверяется, что браузер не делает лишних запросов на Fly states API для карточек, уже покрытых manifest.

Ожидаемый результат:

- карточки показывают актуальные состояния регистрации;
- при штатной работе manifest-first путь не создаёт лишнюю нагрузку на Fly API;
- текст CTA совпадает с фактическим `registration_public_state` и остатком регистрационной квоты.

### H. Fallback на Fly states API при отсутствии manifest

1. Same-origin `/tickets/registration/states.json` временно недоступен или отсутствует.
2. Playwright открывает страницу `/programma/`.
3. Проверяется, что сайт догружает состояния через Fly states API.

Ожидаемый результат:

- пользователь всё равно видит актуальные CTA;
- отсутствие manifest не превращает массово открытые события в ложное состояние `недоступно`.

### I. Честная деградация при одновременной недоступности manifest и states API

1. Same-origin `/tickets/registration/states.json` недоступен.
2. Fly states API тоже временно недоступен.
3. Playwright открывает страницу `/programma/`.

Ожидаемый результат:

- карточка не показывает ложное `Регистрация недоступна`, `Регистрация закрыта` или `Мест нет`;
- кнопка остаётся disabled в честном состоянии `Проверяем регистрацию…`;
- под карточкой допускается нейтральная подсказка о временной недоступности статуса.

### J. Production-start holdback для событий ИЦАЭ

1. Playwright открывает `/programma/` и leaf-страницу `/sobytiya/<slug>/` для события ИЦАЭ.
2. Проверяет, что на карточке и на leaf-странице показывается только дата без точного времени.
3. Проверяет отсутствие публичных строк `ИЦАЭ`, зала и адреса.
4. Проверяет наличие нейтральной строки `Информация о площадке и времени скоро появится.`
5. Проверяет, что CTA принудительно остаётся `Регистрация скоро откроется`.

Ожидаемый результат:

- внутренние данные площадки и вместимости остаются в backend-модели;
- публичная витрина не раскрывает ИЦАЭ до отдельной команды владельца проекта;
- manifest и Fly states API отдают одинаковое публичное состояние holdback.

## 6. Telegram-specific сценарии

### Уведомление о регистрации

1. После браузерной регистрации Telethon проверяет входящее сообщение суперадмину.
2. Проверяются поля:
   - ФИО
   - событие
   - дата и время
   - остаток регистрационной квоты

### Поиск по ФИО

1. Через браузер создаётся регистрация.
2. Через Telethon в бот отправляется `/find <ФИО>`.
3. Проверяется, что бот находит запись.

### Отчёт по событию и XLSX

1. Через бот запрашивается список событий.
2. Выбирается событие.
3. Запрашивается экспорт.
4. В интерфейсе бота проверяется, что телефон и email показаны маскированно.
5. В текстовом отчёте проверяется наличие даты и времени регистрации.
6. В текстовом отчёте проверяется явное поле о согласии на обработку ПДн.
7. Проверяется XLSX:
   - название события
   - дата и время регистрации
   - согласие на обработку ПДн
   - дата и время согласия
   - ФИО
   - почта без маски
   - телефон без маски
   - ссылка на приглашение

### Открытие и закрытие регистрации через Telegram

1. Суперадмин открывает раздел `События` кнопкой.
2. Выбирает конкретное событие кнопкой из списка.
3. Нажимает кнопку `Открыть регистрацию` на event-card.
4. Проверяется, что событие стало доступно для внутренней регистрации и карточка обновилась.
5. Затем суперадмин нажимает кнопку `Закрыть регистрацию`.
6. Проверяется, что регистрация снова недоступна и прямой POST отклоняется.
7. Оператор без прав суперадмина пытается выполнить тот же сценарий.
8. Проверяется, что бот отклоняет действие и не меняет состояние события.

### Help и кнопочная навигация

1. После `/start` бот показывает клавиатуру с основными действиями.
2. По кнопке `Помощь` открывается тот же список команд, что и по `/help`.
3. Для суперадмина видны кнопки открытия/закрытия регистрации.
4. Для суперадмина видна кнопка управления операторами.
5. Для оператора кнопки открытия/закрытия регистрации не показываются.
6. Из раздела `События` суперадмин может дойти до конкретного события и сменить его registration-state кнопкой без ручного ввода команды.

### Daily export и SQL dump

1. Тест принудительно запускает daily job или ждёт тестовый schedule.
2. Проверяет, что суперадмин получил сводный XLSX.
3. Проверяет, что суперадмин получил SQL dump/backup.
4. Проверяет, что задача срабатывает в low-load окне `Europe/Kaliningrad`.

### Self-heal после рестарта приложения

1. В тестовом окружении приложение принудительно перезапускается.
2. Проверяется, что после старта бот снова отвечает на `/help`.
3. Проверяется, что webhook остаётся рабочим без ручной переустановки.
4. Проверяется, что outbox worker и daily scheduler снова активны.

## 7. Сценарии недоступности Telegram

### Кратковременный outage Telegram API

1. В тестовой среде блокируется исходящий доступ приложения к Telegram API.
2. Пользователь проходит регистрацию через браузер.
3. Проверяется, что регистрация и билет созданы успешно.
4. Проверяется, что запись появилась в `telegram_outbox`.
5. Доступ к Telegram API восстанавливается.
6. Проверяется, что накопленное уведомление дошло позже.

Ожидаемый результат:

- регистрация не теряется;
- outbox не застревает навсегда;
- уведомление становится eventual, а не blocking.

### Недоступность webhook endpoint

1. Telegram пытается достучаться до staging webhook.
2. Проверяется `getWebhookInfo` и служебные логи приложения.

Ожидаемый результат:

- команда мониторинга видит `pending_update_count` и последние ошибки;
- после восстановления endpoint бот продолжает работу без переустановки всей системы.

### Аварийный export endpoint

1. Telegram недоступен длительное время.
2. Суперадмин обращается к защищённому read-only export endpoint с секретом в `Authorization` header.
3. Проверяется, что endpoint не даёт менять состояние регистрации.
4. Проверяется, что endpoint не доступен без корректного секрета.

### Очистка тестового прогона

1. Automated run с `test_run_id` создаёт тестовые регистрации.
2. Через Telethon суперадмин запускает `/cleanup_test_run <run_id>`.
3. Проверяется, что тестовые регистрации и связанные ticket artifacts удалены.
4. Проверяется, что `seats_taken` после очистки пересчитан корректно.

## 8. Проверка билета и PDF

1. После успешной регистрации Playwright открывает `/ticket/<public_hash>`.
2. Проверяет, что страница доступна и содержит `short_ticket_id`.
3. Проверяет, что неиндексируемые страницы отдают корректный `noindex`.
4. Проверяет, что на странице есть полный адрес площадки.
5. Проверяет, что на странице есть блок `Добавить в календарь`.
6. Проверяет, что доступны `Android (ICS)`, `Календарь Apple`, `Google`.
7. Проверяет, что `Google` открывает корректно заполненный calendar URL, а `Календарь Apple` и `Android (ICS)` ведут на статический `.ics`-файл билета.
8. Проверяет, что по кнопке скачивается PDF.
9. Проверяет, что в PDF есть `short_ticket_id`, ссылка на `/ticket/<public_hash>`, полный адрес и пометка `Печать билета не требуется`.
10. Проверяет, что пользователь не видит на ticket-page self-service действия отмены регистрации.

## 9. Что именно должно автоматизироваться через Playwright

- Навигация к форме регистрации.
- Работа формы на desktop viewport.
- Работа формы на mobile viewport.
- Проверка manifest-first чтения состояний без лишних запросов на Fly states API.
- Проверка fallback на Fly states API при отсутствии `/tickets/registration/states.json`.
- Проверка честной деградации `Проверяем регистрацию…`, если одновременно недоступны manifest и states API.
- Проверка input mask и UX телефона.
- Проверка ветки `У меня нет отчества`: наличие checkbox, блокировка двухсловного имени без него и успешный проход после явного подтверждения.
- Проверка inline validation.
- Проверка happy path.
- Проверка sold-out и past-event состояний.

## 10. Какие внутренние проверки всё равно нужны в step definitions

- После успешной регистрации step definitions дополнительно проверяют, что ticket HTML, PDF и `.ics` реально доступны по ожидаемым URL. — Статус: `Зафиксировано`
- После успешной регистрации внутренние проверки убеждаются, что в БД нет plaintext `ФИО`, `email` и `телефона`, а есть только шифротекст и blind indexes. — Статус: `Зафиксировано`
- Во время Telegram outage внутренние проверки убеждаются, что уведомление поставлено в `telegram_outbox` и позже доставлено. — Статус: `Зафиксировано`
- В конкурентных сценариях внутренние проверки убеждаются, что `seats_taken` не превысил `registration_limit`. — Статус: `Зафиксировано`
- В сценариях duplicate checks внутренние проверки убеждаются, что в БД не появилась вторая регистрация для того же `event_id`. — Статус: `Зафиксировано`
- В production-like тестовых прогонах внутренние проверки убеждаются, что cleanup по `test_run_id` действительно удалил тестовые строки и вернул счётчики мест. — Статус: `Зафиксировано`

## 11. Критерии приёмки E2E-контура

- Все happy-path сценарии проходят на каждом открытом событии. — Статус: `Зафиксировано`
- Ни один негативный тест не создаёт “битую” регистрацию. — Статус: `Зафиксировано`
- При штатной работе публичный сайт берёт CTA-состояния из same-origin `/tickets/registration/states.json`, а не засыпает Fly API массовыми read-запросами. — Статус: `Не подтверждено пользователем`
- Если `/tickets/registration/states.json` временно недоступен, сайт корректно падает назад на Fly states API. — Статус: `Не подтверждено пользователем`
- Если и manifest, и fallback API одновременно недоступны, интерфейс показывает честное `Проверяем регистрацию…`, а не ложное `Регистрация недоступна`. — Статус: `Не подтверждено пользователем`
- При конкурентной записи система никогда не отдаёт больше билетов, чем разрешено текущей регистрационной квотой. — Статус: `Зафиксировано`
- При Telegram outage регистрация продолжает работать. — Статус: `Зафиксировано`
- После восстановления Telegram backlog уведомлений догоняется. — Статус: `Зафиксировано`
- Event-level экспорт доступен из бота, а общий экспорт и SQL dump доступны суперадмину. — Статус: `Зафиксировано`
- Ticket page даёт понятные calendar actions для Google и `.ics`-сценариев. — Статус: `Зафиксировано`
- Оператор не может открывать/закрывать регистрацию, а суперэкспорты и аварийный endpoint доступны только суперадмину. — Статус: `Зафиксировано`
- После рестарта приложения Telegram-контур самовосстанавливается без ручной перенастройки webhook. — Статус: `Зафиксировано`
- Пользовательский submit-flow никогда не оставляет интерфейс в безмолвном состоянии после нажатия на кнопку регистрации. — Статус: `Зафиксировано`
- Двухсловное имя не уходит на submit без явного подтверждения отсутствия отчества, а сценарий `У меня нет отчества` остаётся быстрым и понятным на desktop и mobile. — Статус: `Зафиксировано`

## 12. Операционный runbook

- Быстрый вход по документам для следующего прохода централизован в [docs/README.md](/workspaces/kdg80/docs/README.md). — Статус: `Зафиксировано`
- Стабильная точка запуска production-like preview E2E вынесена в [scripts/run_registration_preview_e2e.py](/workspaces/kdg80/scripts/run_registration_preview_e2e.py), а не только в разовые файлы из `test-results/`. — Статус: `Зафиксировано`
- Перед browser E2E secret preview должен быть опубликован на `kgd80.ru/preview-.../` через [deploy-preview-to-yc.sh](/workspaces/kdg80/deploy-preview-to-yc.sh). — Статус: `Зафиксировано`
- Артефакты каждого прогона должны складываться в отдельную папку `test-results/registration-e2e-preview-<timestamp>/` или эквивалентную. — Статус: `Зафиксировано`
- Если в среде нет `aws`, допустим локальный временный venv только для `awscli`, без изменения проектового runtime. — Статус: `Зафиксировано`
- Если Telethon-сессия видит бот, но не проходит admin-only проверки, перед повторной диагностикой нужно определить её Telegram user id и сверить/при необходимости добавить запись в `telegram_admins` через Fly SSH и production SQLite. — Статус: `Зафиксировано`
- Для визуальной приёмки одного production-like прогона нужен не только `summary.png`, но и отдельные скрины карточек программы со статусами `Регистрация`, `Регистрация скоро откроется`, `Регистрация закрыта`, `Мест нет`, а также скрины inline validation, consent-блока и успешной ticket-page. — Статус: `Не подтверждено пользователем`
- Перед mutating-проверками на Fly нужно проверить текущий `REGISTRATION_OPERATING_MODE`; пока он равен `testing`, cleanup и контролируемые status toggles допустимы, при `production` такие действия не выполняются без отдельной команды владельца. — Статус: `Зафиксировано`
- Закрывающий отчёт по тестированию всегда должен содержать актуальный secret preview URL вида `https://kgd80.ru/preview-.../`. — Статус: `Зафиксировано`

Рекомендуемая последовательность:

```sh
python3 -m venv /tmp/awscli-venv
/tmp/awscli-venv/bin/pip install awscli
PATH="/tmp/awscli-venv/bin:$PATH" YC_PUBLIC_BASE_URL="https://kgd80.ru" ./deploy-preview-to-yc.sh
.venv-e2e/bin/playwright install chromium
.venv-e2e/bin/python scripts/run_registration_preview_e2e.py --base-url "https://kgd80.ru/preview-<slug>/"
```

Operational note:

- Если secret preview уже новый, но live ticket HTML на `/tickets/<public_hash>/` показывает старые calendar labels или старую ticket-разметку, нужно проверять не preview deploy, а версию registration backend на Fly.io.
